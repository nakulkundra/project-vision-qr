## 2026-08-05 - Inline Validation Accessibility
**Learning:** Replacing native browser `alert()` with an inline error message linked to an input element using `aria-describedby`, combined with setting `role="alert"` and `aria-invalid="true"`, creates an accessible validation pattern that provides immediate context for screen readers.
**Action:** Apply this pattern when replacing other intrusive alert-based validations.
